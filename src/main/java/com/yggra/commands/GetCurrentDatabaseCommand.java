package com.yggra.commands;

/**
 * 🧭 [RUNIC INSCRIPTION] 🧭
 * Seeks knowledge of the current realm under the executor's influence (GET CURRENT DATABASE).
 * This command does not require parameters — it simply invokes the oracle,
 * asking: "Where do I stand within the branches of Yggdrasil?"
 */

public class GetCurrentDatabaseCommand  extends SQLCommand {
    // 🕊️ No fields are required; the mere invocation reveals the path.
}
